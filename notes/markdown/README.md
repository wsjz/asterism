# Markdown output

Normalized copies from configured local Markdown directories are written here.
This includes Obsidian vaults and other products that store notes as `.md` files.

Collected files are ignored by Git in this development repository. Input roots
must not overlap the Asterism output vault, which prevents recursive collection.
