# Notion output

Notion pages collected through the official API are written here as Markdown.
The default configuration requires explicit root page or data source IDs;
workspace-wide discovery must be deliberately enabled.

Collected files are ignored by Git in this development repository. API tokens
belong in `ASTERISM_NOTION_TOKEN`, never in the project configuration.
